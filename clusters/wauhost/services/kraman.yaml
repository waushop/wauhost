apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kraman
  namespace: flux-system
spec:
  releaseName: web
  targetNamespace: kraman
  interval: 30m
  chart:
    spec:
      chart: ./services/kraman
      reconcileStrategy: Revision
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
