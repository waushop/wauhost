ghost:
  domain: "{{ .Values.global.domain }}"
  database:
    host: mysql
    user: ghost
    password: "{{ .Values.secrets.mysql.password }}"
  ingress:
    enabled: true
    class: "{{ .Values.global.ingressClass }}"
    tlsIssuer: "{{ .Values.global.tlsIssuer }}"
