apiVersion: v1
kind: Service
metadata:
  name: {{ include "kraman.fullname" . }}
  labels:
    {{- include "kraman.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "kraman.selectorLabels" . | nindent 4 }}
