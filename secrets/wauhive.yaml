# wauhive sealed secrets — placeholder.
#
# This file MUST contain two SealedSecret resources before Flux reconciles
# wauhive successfully:
#
#   1. wauhive-secrets   — WAUHIVE_PASSWORD + ANTHROPIC_API_KEY (mounted via
#      envFrom on the backend Deployment).
#   2. ghcr-pull-secret  — dockerconfigjson for ghcr.io/waushop/wauhive-*.
#
# See services/wauhive/README.md for the kubeseal commands to generate them.
# Until both are sealed and committed here, the backend pod will fail to
# start (missing envFrom secret) or fail to pull the image.
