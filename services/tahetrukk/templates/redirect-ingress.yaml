apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "tahetrukk.fullname" . }}-www-redirect
  labels:
    {{- include "tahetrukk.labels" . | nindent 4 }}
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt"
    traefik.ingress.kubernetes.io/router.middlewares: default-redirect-www-tahetrukk@kubernetescrd
spec:
  ingressClassName: {{ .Values.ingress.className }}
  tls:
    - hosts:
        - www.tahetrukk.ee
      secretName: tahetrukk-www-tls
  rules:
    - host: www.tahetrukk.ee
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "tahetrukk.fullname" . }}
                port:
                  number: {{ .Values.service.port }}
---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: redirect-www-tahetrukk
  namespace: default
spec:
  redirectRegex:
    regex: ^https://www\.tahetrukk\.ee/(.*)
    replacement: https://tahetrukk.ee/${1}
    permanent: true