apiVersion: v1
kind: Service
metadata:
  name: {{ include "wauhive.fullname" . }}-backend
  labels:
    {{- include "wauhive.componentLabels" (merge (dict "component" "backend") .) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "wauhive.componentSelectorLabels" (merge (dict "component" "backend") .) | nindent 4 }}
