apiVersion: v1
kind: Service
metadata:
  name: {{ include "onebetwonder.fullname" . }}
  labels:
    {{- include "onebetwonder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "onebetwonder.selectorLabels" . | nindent 4 }}
